---
phase: 01-layout-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/feed/page.tsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "Homepage header shows only the title — no avatar circle, no 'TRACK YOUR BUILDS' subtitle"
    - "Neither homepage nor feed page shows a fixed bottom navigation bar"
    - "Feed page shows all ships with no filter buttons visible"
    - "Submit button on homepage displays ship emoji, not rocket emoji"
    - "Heatmap card header has no 'past year' descriptor text"
  artifacts:
    - path: "app/page.tsx"
      provides: "Cleaned homepage — avatar removed, subtitle removed, nav removed, emoji swapped, past-year label removed"
    - path: "app/feed/page.tsx"
      provides: "Cleaned feed page — filter state removed, filter buttons removed, nav removed, always renders all ships"
  key_links:
    - from: "app/feed/page.tsx"
      to: "ships state"
      via: "filtered variable replaced by direct ships reference"
      pattern: "filtered\\.map|ships\\.map"
---

<objective>
Remove all six clutter elements from homepage and feed page: user avatar, subtitle text, bottom navigation bar (both pages), feed filter buttons + state, and swap the submit button emoji from rocket to ship.

Purpose: Clean up visual noise so the interface focuses entirely on ship entries and stats.
Output: app/page.tsx and app/feed/page.tsx with the six elements removed/changed.
</objective>

<execution_context>
@C:/Users/Aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-layout-and-cleanup/01-CONTEXT.md

<interfaces>
<!-- Key code contracts the executor needs. Extracted from source files. -->

From app/page.tsx (header section, lines 35-48):
```tsx
<header className="flex items-center justify-between pt-6 pb-4">
  <div>
    <div className="text-2xl font-bold">
      <span className="text-ship-accent">ship</span>
      <span className="text-ship-text">log</span>
    </div>
    <div className="text-[10px] tracking-widest text-ship-text mt-0.5">
      TRACK YOUR BUILDS
    </div>               {/* REMOVE: CLEAN-02 */}
  </div>
  <div className="w-9 h-9 rounded-full bg-ship-accent flex items-center justify-center text-white font-bold text-sm">
    A
  </div>                 {/* REMOVE: CLEAN-01 */}
</header>
```

From app/page.tsx (heatmap card header, lines 52-55):
```tsx
<div className="flex items-center justify-between mb-3">
  <span className="text-xs text-ship-text">2025 Shipping</span>
  <span className="text-[10px] text-ship-text">past year</span>  {/* REMOVE */}
</div>
```

From app/page.tsx (submit button, line 73):
```tsx
🚀 Submit a Ship   {/* CHANGE to: 🚢 Submit a Ship */}
```

From app/page.tsx (bottom nav, lines 91-99):
```tsx
<nav className="fixed bottom-0 left-0 right-0 border-t border-ship-border bg-ship-bg z-30">
  ...
</nav>               {/* REMOVE: CLEAN-03 */}
```

From app/feed/page.tsx (filter state, line 11):
```tsx
const [filter, setFilter] = useState<"all" | "screenshots">("all");  {/* REMOVE: CLEAN-04 */}
```

From app/feed/page.tsx (filtered derived state, lines 29-32):
```tsx
const filtered =
  filter === "screenshots"
    ? ships.filter((s) => s.screenshot_url)
    : ships;              {/* REMOVE: replace all 'filtered' refs with 'ships' */}
```

From app/feed/page.tsx (filter buttons UI, lines 48-71):
```tsx
{/* Filter buttons */}
<div className="flex gap-2">
  <button onClick={() => setFilter("all")} ...>All</button>
  <button onClick={() => setFilter("screenshots")} ...>With 📷</button>
</div>                 {/* REMOVE entire block: CLEAN-04 */}
```

From app/feed/page.tsx (bottom nav, lines 99-108):
```tsx
<nav className="fixed bottom-0 left-0 right-0 border-t border-ship-border bg-ship-bg z-30">
  ...
</nav>               {/* REMOVE: CLEAN-03 */}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up app/page.tsx — remove avatar, subtitle, bottom nav, past-year label; swap emoji</name>
  <files>app/page.tsx</files>
  <action>
Make these five targeted edits to app/page.tsx:

1. CLEAN-01 — Remove the avatar div (lines 45-47): Delete the entire `<div className="w-9 h-9 rounded-full bg-ship-accent ...">A</div>` element. The header `flex items-center justify-between` wrapper can stay; with only the title div remaining, change the header className to `"pt-6 pb-4"` (drop `justify-between` since there is nothing to justify against).

2. CLEAN-02 — Remove the subtitle: Delete `<div className="text-[10px] tracking-widest text-ship-text mt-0.5">TRACK YOUR BUILDS</div>`.

3. "past year" label — In the heatmap card header, remove `<span className="text-[10px] text-ship-text">past year</span>`. The `flex items-center justify-between` wrapper can be simplified to `flex items-center mb-3` since there is nothing on the right side now.

4. CLEAN-05 — Change the submit button text: replace `🚀 Submit a Ship` with `🚢 Submit a Ship`.

5. CLEAN-03 — Remove the entire bottom `<nav className="fixed bottom-0 left-0 right-0 ...">...</nav>` block (lines 91-99). Also change `pb-20` class references if any remain from the nav's presence — but note `pb-20` is on `app/layout.tsx`, not this file.

After edits, the `useEffect`, `fetchShips`, state, and all component imports remain unchanged — only the DOM elements listed above are removed/changed.
  </action>
  <verify>
    <automated>cd C:/Users/Aaron/Desktop/claude/shiplog && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>app/page.tsx has no avatar div, no "TRACK YOUR BUILDS" text, no "past year" span, no bottom nav block, and the submit button reads "🚢 Submit a Ship". Build passes with no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up app/feed/page.tsx — remove filter state, filter UI, bottom nav; always show all ships</name>
  <files>app/feed/page.tsx</files>
  <action>
Make these three targeted edits to app/feed/page.tsx:

1. CLEAN-04 — Remove filter state: Delete `const [filter, setFilter] = useState<"all" | "screenshots">("all");` from the state declarations. Remove the `filtered` derived variable (the `const filtered = filter === "screenshots" ? ...` block). Replace every reference to `filtered` in the JSX with `ships` directly (the map on line 93 and the conditional count display on line 79).

2. CLEAN-04 — Remove filter buttons UI: Delete the entire `{/* Filter buttons */}` block — the `<div className="flex gap-2">` with both `<button>` elements inside it (lines 48-70). The header can remain but without anything on the right side; simplify its className from `flex items-center justify-between` to `flex items-center gap-3` (or just keep the back arrow and title).

3. CLEAN-03 — Remove the bottom nav: Delete the entire `<nav className="fixed bottom-0 left-0 right-0 ...">...</nav>` block (lines 99-108).

Also remove the stale conditional `{filtered.length !== ships.length && (...)}` display (line 79-81) since filter is gone — the count section simplifies to just the total ships count.

After edits, remove unused `useState` import only if `filter` was the last state variable — but `loading` and `ships` state remain, so `useState` stays imported.
  </action>
  <verify>
    <automated>cd C:/Users/Aaron/Desktop/claude/shiplog && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>app/feed/page.tsx has no filter state, no filter buttons, no bottom nav. The ship list always renders all ships. Build passes with no TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npm run build` passes with zero TypeScript errors
- app/page.tsx: grep confirms no "TRACK YOUR BUILDS", no "w-9 h-9", no "fixed bottom-0", has "🚢"
- app/feed/page.tsx: grep confirms no "setFilter", no "With 📷", no "fixed bottom-0"
</verification>

<success_criteria>
Both pages compile cleanly. Visual clutter elements (avatar, subtitle, nav bars, filter buttons, past-year label) are absent from the source. Ship emoji is 🚢 on the submit button.
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-and-cleanup/01-01-SUMMARY.md` following the summary template.
</output>
