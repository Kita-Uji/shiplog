---
phase: 01-layout-and-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - tailwind.config.ts
  - components/Heatmap.tsx
  - components/ShipCard.tsx
autonomous: false
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - CLEAN-06

must_haves:
  truths:
    - "Heatmap shows approximately 3-4 months of data (14-16 weeks), not 52 weeks"
    - "Heatmap fits within its container — no horizontal scrollbar"
    - "App title is visually large (~text-6xl), dominating the header"
    - "Overall interface feels ~125% larger — container, text, spacing, and cells all scaled up"
    - "Heatmap empty cells and ship post card backgrounds are noticeably lighter than before"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Wider container (max-w-2xl, no pb-20)"
    - path: "app/page.tsx"
      provides: "Title at text-6xl"
    - path: "tailwind.config.ts"
      provides: "Lightened ship-card color (~10% lighter than #F0F0E8)"
    - path: "components/Heatmap.tsx"
      provides: "WEEKS=16, larger cells (w-4 h-4), updated inline empty-cell color"
    - path: "components/ShipCard.tsx"
      provides: "Proportionally scaled padding/text to match new container width"
  key_links:
    - from: "tailwind.config.ts"
      to: "components/ShipCard.tsx"
      via: "ship-card color token used as bg-ship-card"
      pattern: "bg-ship-card"
    - from: "app/layout.tsx"
      to: "all pages"
      via: "max-w container wraps entire app"
      pattern: "max-w-2xl"
---

<objective>
Scale up the layout to ~125% and resize the heatmap to 3-4 months. Adjust container width, title size, cell dimensions, spacing, and lighten background colors.

Purpose: The interface should feel larger and give more visual weight to ship entries while the heatmap focuses on recent activity rather than a full year.
Output: Wider container, larger title, shorter heatmap, lighter backgrounds, proportionally scaled components.
</objective>

<execution_context>
@C:/Users/Aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-layout-and-cleanup/01-CONTEXT.md
@.planning/phases/01-layout-and-cleanup/01-01-SUMMARY.md

<interfaces>
<!-- Key code contracts the executor needs. Extracted from source files. -->

From app/layout.tsx (line 17) — current container:
```tsx
<div className="max-w-lg mx-auto px-4 pb-20">{children}</div>
```
Target: Change `max-w-lg` to `max-w-2xl`. Change `pb-20` to `pb-8` (bottom nav removed in Plan 01, no longer needed as spacer; a small bottom padding keeps content from touching viewport edge).

From app/page.tsx (title, lines 37-40) — current title after Plan 01:
```tsx
<div className="text-2xl font-bold">
  <span className="text-ship-accent">ship</span>
  <span className="text-ship-text">log</span>
</div>
```
Target: Change `text-2xl` to `text-6xl` (LAYOUT-03: 250% of current size).

From tailwind.config.ts — current ship-card color:
```ts
"ship-card": "#F0F0E8",
```
Target: Lighten by ~10% to approximately `#F5F5EF` (CLEAN-06). Claude's discretion on exact hex. The heatmap empty cell color `#E8E8E0` (used inline in Heatmap.tsx) should also be lightened by ~10% to approximately `#EEEEEA`.

From components/Heatmap.tsx — current constants and cell sizes:
```tsx
const WEEKS = 52;
// cells: className={`w-3 h-3 rounded-sm ...`}
// month label divs: className="w-3 flex-shrink-0 ..."
// legend cells: className={`w-3 h-3 rounded-sm ${c}`}
// empty null cells: className="w-3 h-3 rounded-sm"
// getCellColor(0) returns "bg-[#E8E8E0]"  (also appears in legend array)
```
Targets:
- `WEEKS = 52` to `WEEKS = 16` (LAYOUT-01: ~4 months)
- All `w-3 h-3` cell classes to `w-4 h-4` (scale up cells with overall layout)
- All `w-3 flex-shrink-0` month label wrapper divs to `w-4 flex-shrink-0`
- The inline color `#E8E8E0` in getCellColor and in the legend array to `#EEEEEA` (lightened, CLEAN-06)

From components/ShipCard.tsx — current card padding:
```tsx
<div className="bg-ship-card rounded-xl border border-ship-border overflow-hidden">
  <div className="p-4">  {/* card body padding */}
```
The card already uses `bg-ship-card` which gets lightened via Tailwind config — no explicit color change needed here. Consider scaling `p-4` to `p-5` and `text-base` title to `text-lg` to feel proportional in the wider container. Claude's discretion on exact values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scale container, title, and lighten background colors</name>
  <files>app/layout.tsx, app/page.tsx, tailwind.config.ts</files>
  <action>
Three targeted file edits:

app/layout.tsx: Change line 17 from `max-w-lg mx-auto px-4 pb-20` to `max-w-2xl mx-auto px-4 pb-8` (max-w-lg to max-w-2xl for LAYOUT-02/LAYOUT-04 scale-up; pb-20 to pb-8 since bottom nav was removed in Plan 01).

app/page.tsx: Change the title wrapper from `text-2xl font-bold` to `text-6xl font-bold` (LAYOUT-03: ~250% of text-2xl).

tailwind.config.ts: Change `"ship-card": "#F0F0E8"` to `"ship-card": "#F5F5EF"` (CLEAN-06: ~10% lighter). Leave all other color tokens unchanged.
  </action>
  <verify>
    <automated>cd C:/Users/Aaron/Desktop/claude/shiplog && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Container uses max-w-2xl, title uses text-6xl, ship-card color is lightened to approximately #F5F5EF. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Resize heatmap to 16 weeks with larger cells and lightened empty-cell color</name>
  <files>components/Heatmap.tsx, components/ShipCard.tsx</files>
  <action>
components/Heatmap.tsx — four changes:

1. Line 11: `const WEEKS = 52;` to `const WEEKS = 16;` (LAYOUT-01: ~4 months)

2. Cell sizing — every `w-3 h-3` occurrence to `w-4 h-4`. This applies to: active/empty cell divs in the grid render, the null placeholder cell, and the legend cells.

3. Month label column width — every `w-3 flex-shrink-0` in the month label row to `w-4 flex-shrink-0` (line 79) so labels stay aligned with cells.

4. Empty cell color — change `#E8E8E0` to `#EEEEEA` in two places (CLEAN-06): in the getCellColor function return value and in the legend array.

components/ShipCard.tsx — proportional scale adjustments (LAYOUT-04):
- Card body: `p-4` to `p-5`
- Title: `text-base` to `text-lg`
- Date text: `text-xs` to `text-sm`

The `bg-ship-card` class automatically picks up the lightened color from tailwind.config.ts — no color change needed in this file.
  </action>
  <verify>
    <automated>cd C:/Users/Aaron/Desktop/claude/shiplog && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Heatmap renders 16 weeks with w-4 h-4 cells. Empty cell color is lightened. ShipCard has proportionally larger text and padding. Build passes with zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of scale and layout changes</name>
  <files>none</files>
  <action>No code changes — human verifies the visual result of Tasks 1 and 2.</action>
  <verify>Human confirms layout looks correct at localhost:3000 and localhost:3000/feed.</verify>
  <done>User types "approved" confirming the scaled layout meets expectations.</done>
  <what-built>
    Full visual pass: container widened to max-w-2xl, title enlarged to text-6xl, heatmap shows ~4 months with larger cells, ship card backgrounds and heatmap empty cells are lighter, ShipCard text/padding scaled up.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` in C:/Users/Aaron/Desktop/claude/shiplog
    2. Visit http://localhost:3000
    3. Check: title "shiplog" is large (roughly 3x bigger than before)
    4. Check: the interface feels wider/larger overall
    5. Check: heatmap shows ~4 months of cells (not a full year)
    6. Check: heatmap cells are slightly larger than before
    7. Check: heatmap background/empty cells and ship cards look slightly lighter/warmer
    8. Check: no horizontal scroll on the heatmap
    9. Visit http://localhost:3000/feed
    10. Check: wider layout, no filter buttons, all ships shown
  </how-to-verify>
  <resume-signal>Type "approved" if the layout looks right, or describe any adjustments needed (e.g., "title too big", "container needs to be wider")</resume-signal>
</task>

</tasks>

<verification>
After Tasks 1 and 2 complete (before checkpoint):
- `npm run build` passes with zero TypeScript errors
- Confirm: `grep -n "max-w-2xl" app/layout.tsx` finds the container class
- Confirm: `grep -n "text-6xl" app/page.tsx` finds the title class
- Confirm: `grep -n "WEEKS = 16" components/Heatmap.tsx` finds the constant
- Confirm: `grep -n "F5F5EF" tailwind.config.ts` finds lightened ship-card color
</verification>

<success_criteria>
Build passes. Container is max-w-2xl. Title is text-6xl. Heatmap shows 16 weeks with w-4 h-4 cells. Background colors are lightened. Human approves the visual result at checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-and-cleanup/01-02-SUMMARY.md` following the summary template.
</output>
